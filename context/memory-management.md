# Memory Management

- When you learn important information from conversations with the user, record it in `context/memory.md` file.
- When user uses expressions like "remember", "don't forget", you MUST record it.
- Record user's name, preferences, project information, recurring request patterns, etc.
- Delete information that is no longer valid.
